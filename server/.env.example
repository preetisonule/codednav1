# Server
PORT=5000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# GitHub REST API
# Optional but recommended: raises the rate limit from 60/hr to 5000/hr
GITHUB_TOKEN=

# AI Provider (future-ready abstraction — see server/src/services/ai.service.ts)
AI_PROVIDER=mock
OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
